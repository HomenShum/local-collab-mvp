# NodeVoice current controls evidence

Read the root HANDOFF first. The two-owner source repair and the original external evidence proposal were independently reviewed; this packet records that local proof. Shared CI and public deployment for this repair remain separate. The packet preserves selected raw evidence for the local deterministic comparison and room-control layout repair; it does not certify a hosted or provider-backed product.

Run `python verify.py` here with Python 3 standard library only. Optional `--source-root /path/to/checkout` checks the exact historical 177-file reviewed source binding. This is raw byte preservation, not a replay, authenticated provenance, staged/filter equality or a full readiness grade. The raw Git blob hashes are calculated from `blob <length>\0` followed by the exact bytes, without repository filters.

Claims and direct evidence:

- [Final 337-check / 94-capture report](raw/E6j-nodevoice-ui-controls-after-04/browser-01/report.json) binds actual native 100 after 125.736 s and Run re-enabled after 126.829 s within 300 s. [Same native request/API](raw/E6j-nodevoice-ui-controls-after-04/browser-01/same-run-api.json) retains deterministic provenance. [Actual 320 completed pixels](raw/E6j-nodevoice-ui-controls-after-04/browser-01/320-demo-settled100.png) and [390-pixel doubled-text result](raw/E6j-nodevoice-ui-controls-after-04/browser-01/390-demo-settled100-computed-text200.png) show the repaired status.
- [Reverse keyboard at 360 pixels](raw/E6j-nodevoice-ui-controls-after-04/browser-01/360-rail-first-keyboard.png), [390-pixel Invite keyboard](raw/E6j-nodevoice-ui-controls-after-04/browser-01/390-waiting-room-invite-keyboard.png), and their structured DOM/font records cover the scoped visual decisions. Every assertion and captured viewport remains in the raw report. Some duplicate pointer PNGs and all complete application HTML are retained locally rather than copied.
- [Doctor](raw/E6j-nodevoice-ui-repair-01/doctor-04.log.txt), [proof: 55 tests / 9 files plus CLI](raw/E6j-nodevoice-ui-repair-01/proof-04.log.txt), [build](raw/E6j-nodevoice-ui-repair-01/build-04.log.txt), and [original normal install](raw/E6j-nodevoice-current-baseline-01/install.log.txt) retain the actual commands' outputs. [Raw locked audit](raw/E6j-nodevoice-baseline-independent-02/npm-audit.stdout.json) remains a dependency hold.
- [Layout-only source reconstruction](raw/E6j-nodevoice-ui-repair-01/layout-only-guard.json) and [current source/build hashes](raw/E6j-nodevoice-ui-final-freeze-01/current-source-dist.json) preserve identity. This source inventory is the historical 177 files, not a claim that proposed HANDOFF/packet files were part of the observed runtime.
- [Failed forward Tab](raw/E6j-nodevoice-ui-controls-after-02/browser-01/report.json), [failed reverse Tab](raw/E6j-nodevoice-ui-controls-after-03/browser-01/report.json), [failed margin diagnostic](raw/E6j-nodevoice-rail-scroll-diagnostic-02/browser-01/report.json), and [passing wrapping diagnostic](raw/E6j-nodevoice-rail-wrap-diagnostic-01/browser-01/report.json) preserve the causal sequence. The first after-run raw PASS used an inadequate interior-only oracle; its report and scoped correction remain distinct.

[raw-copy-map.json](raw-copy-map.json) verifies exact origin-to-payload identities. [operator-custody-map.json](operator-custody-map.json) lists the frozen 1,075 original operator artifacts and identifies omissions; omitted originals remain local under their hashes. [runtime-summary.json](runtime-summary.json) is an explicitly whitelisted derivative of the raw runtime receipt, excluding environment-name inventories. It retains original hash, status, health flags, listener identity and closure checks. Raw receipts are unchanged and operator-local.

Omitted full HTML means a new recipient cannot reconstruct arbitrary hidden attributes/scripts/full DOM or replay the exact old browser session from the selected PNGs and structured state. The inert recorder source is historical and contains operator-specific paths; it is not advertised as a portable launcher. No private environment files, browser profiles, authentication/session stores or real rooms are included.

Full grades remain null. Main panes, option-label ellipsis, short-height reading, native zoom, physical devices, microphone/audio quality, real provider/agent behavior, hosted/durable rooms and the three high audit package entries remain open. Existing local listener behavior is not loopback-only. No change to playback or room handlers is claimed.

Independent reviews: [current source and rendered evidence](raw/E6j_NODEVOICE_UI_REPAIR_FINAL_JUDGE.md.txt), and [the original external handoff proposal](raw/E6j_NODEVOICE_EXTERNAL_PUBLICATION_JUDGE.md.txt). The four added review records are exact copies outside the earlier 1,075-artifact universe; that historical omission map is unchanged.

Six historical command logs retain exact bytes with a `.log.txt` suffix so the repository's existing log-ignore rule does not omit them from normal Git publication. Their copy and custody maps record the portable names.
