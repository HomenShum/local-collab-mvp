# NodeVoice current first-use judgment

**Builder — accepted as current functional first-use evidence, with UI, handoff and full-grade holds.** A developer can reproduce the deterministic comparison and see the room fail honestly without a provider key. Phone users still lose visible controls and completion information, so this is not complete usage readiness.

**Re your request:** execute planner-worker-judge consolidation and make each repo ready for usage and developer handoff — this independent review binds the actual current first-use evidence, its failures and its remaining limitations.

- Exact source`d225bcaf64008ee522bc66d40aa72eb0381b95a6`/tree`4806fb33...`:177 actual Git/working files45,514,478 bytes,3 built/served files465,063 bytes,174 current artifacts11,475,390 bytes and147 first-failure artifacts verified. Candidate/primary HEAD, refs, index entries and status are unchanged; original primary remains2ce2d4b and draftPR4 is preserved.
- One real browser POST used target100/turns100/source deterministic/useOllama=false. The same response has100 ordered committed states and final completed100, with executed model null and fallbackTurns0. Native samples observed1…100; visible100 at123.564s and enabled Run at125.203s, within the original300s budget. The final DOM records speaking=false/pending=false. This proves completion state and playback lifecycle, not human-audited sound or statistical latency.
- Two clean contexts joined distinct seats. Native correction changed next to5/target10 and auto-resumed into one missing-key error. Explicit Start produced one distinct additional error. The actual room remained halted, turn0/next5; both error IDs persisted after explicit navigation in the same server memory. Source confirms missing-key validation precedes provider fetch and the scheduler catches/halt reports that failure. Neither HTTP200 nor selected model metadata is counted as completed provider work.
- The original font-blocked run remains FAIL with all147 artifacts unchanged. The corrected recorder permits only the source stylesheet and its exact referenced public font URLs:15 HTTP200 responses, three distinct payloads(one CSS/two font files). Every one of36 capture sets has exact viewport PNG/DOM/font evidence and native CDP custom-glyph observations. There was one intentional EventSource close on navigation and no unexpected browser errors/blocked egress. Public font requests mean this is not a zero-network run.

I directly viewed8 retained PNGs. The390 room Invite button is clipped9.3125px;320 demo completion badge and narrow side-by-side state content clip. At390 doubled computed text, count/completion and much of the detail are cut off;1440 doubled header controls truncate labels/numbers. Zero document scrollWidth overflow does not negate clipped descendants. Desktop shows both missing-key errors and Start; phone error/Start visibility and below-fold enlarged lobby keyboard/scroll reachability remain unproved. These are explicit open findings, not passed visual grades.

The listener actually bound`::` and`0.0.0.0` on4993; it was not loopback-only. Owned PID/time identities are closed and4993 remains unoccupied. Names-only isolation, health and pre-fetch source paths support the no-provider scope; there is no server packet-capture proof. No microphone, hosted Convex, provider completion, durable restart, physical device, native zoom, full-product or production acceptance is inferred. Current three high package findings remain held.

The21 raw checks include repeated polling assertions, not21 independent user scenarios. This reviewer reran no application/browser command and introduced no third125s run. One exploratory reader used Windows' default text encoding; it was corrected to explicit UTF-8 without changing data or rerunning the product.

Raw packet remains operator-local pending a separate privacy/custody publication review and new product HANDOFF. All eight full dimension scores remain null. The bounded two-owner UI proposal is a separate next decision; it must retain these actual before records, clearly label any recorded-response boundary representation, and produce a new actual native after completion.

Evidence: [independent assertions](E6j-nodevoice-first-use-independent-02/checks.json), [actual source Git readback](E6j-nodevoice-first-use-independent-02/source177-git-readback.json), [same-run causal summary](E6j-nodevoice-first-use-independent-02/same-run-causal-summary.json), [pixel review](E6j-nodevoice-first-use-independent-02/independent-pixel-review.json), [glyph summary](E6j-nodevoice-first-use-independent-02/actual-glyph-summary.json), [closed custody](E6j-nodevoice-first-use-independent-02/closed-custody.json), [manifest](E6j-nodevoice-first-use-independent-02/manifest.json) SHA256`1d7ce27ad380370c4d517c43eac7e80d8f699c5c572867a3a81f95c1e5e31ac2`. [Original full receipt](E6j_NODEVOICE_CURRENT_FIRST_USE_FINAL_RECEIPT.json) remains unchanged.

Guard CLOSED; nonauthor reviewer context reused. No product/index/ref changes, provider/private access or runtime replay by the judge.
