# NodeVoice controls layout plan judgment

**Builder — approved with the explicit before-evidence amendment below.** A phone participant must reach Invite and State, and a developer using the comparison must reach its native inputs and read the completed count. The current screenshots show these controls/statuses clipped by their owning layout containers. Fix those containers in the existing two owners.

**Re your request:** make individual repositories ready for use and handoff, including visual, responsive and interaction standards — this approves one bounded next repair, not the entire UI or a grade upgrade.

The frozen plan is `E6j_NODEVOICE_UI_FOLLOWON_PLAN.json`, SHA256 `4a3b670b67189f67fa15c9cf402f079501bbc3883f533f425cd1f5c637d6a3ab`. Actual source supports its causal chain: the room header and action group do not wrap inside a locked shell; the comparison rail right-aligns shrinking controls and hides its scrollbar; the progress label/count/badge stay in one line; numeric fields have fixed height/width. The existing container owners are sufficient for the proposed first attempt.

Only `src/client/App.tsx` and `src/client/live/LiveRoom.tsx` may change, in the named header/control rail/NumberField/progress regions. Keep handlers, playback, model/source visibility policy, room reducer/provider behavior and shared Select/Badge/Button implementations unchanged. Native horizontal scrolling with a named group is appropriate for the existing rail; verify its first and last controls are reachable. Numeric `100` and its native spinner must fit at the enlarged text size. Do not claim full option-label readability where the existing selected-label ellipsis remains.

The worker clarified the before method: **annotate the exact frozen native-run PNGs in an inert viewer**, using native DOM 3px boundaries; bind the original PNG hash, verify the unlabelled raster matches its source pixels, then remove overlays and verify viewer DOM/raster restoration. Label these as annotations of frozen native captures, never a fresh application render. This supersedes the proposed retained-API-body replay; no API response or playback clock needs changing, and no extra count-to-100 run is needed for boundary annotation.

Correct the before-cell inventory precisely: retained room befores are390/1440 normal captures. There is no retained320 or text200 room before in this packet. Existing demo befores cover320/390/1440 normal and390/1440 doubled text. Annotate only actual available captures. All-seven room widths and affected enlarged controls remain legitimate **new after** proof obligations; do not invent matched before cells.

After the patch, run the ordinary doctor/proof/build commands and one new native current-source100/100 comparison under the unchanged300s deadline, including Run re-enabled. Use that actual settled state across the seven exact viewports. Capture same-cell DOM, console, PNG and native font evidence before each resize. Verify the scoped count/badge and Invite/State boxes, hit targets and focus; rail pointer scrolling and Tab/Shift+Tab must reach every rendered input without selecting a live provider. Exercise native Invite/State open/close, repeated and burst toggles, preserving room task state. Retain the two-context correction1/error1 plus explicit Start/new-error1 and turn0/next5 behavior. Clearly distinguish a represented old state from this fresh native completion.

Stop if the patch needs a new owner, handler change, shared primitive redesign or causes affected controls to be occluded by the extra header height. Freeze the actual failure and propose a narrow amendment. Comparison panes, detail scrolling/density, selected-label ellipsis, provenance copy, audit findings, unspecified listener host, handoff publication and full grades remain outside this two-owner repair.

The unchanged first-use guard is closed in [the independent first-use judgment](E6j_NODEVOICE_CURRENT_FIRST_USE_JUDGE.json). This plan approval does not reopen or relabel the first failed font run, certify full responsive readiness, or authorize provider/deployment work.
