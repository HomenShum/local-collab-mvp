# NodeVoice external dependency candidate

A developer can build and check this exact local demo with the seven reviewed tooling updates. The ordinary install, audits, doctor, 55-test proof and build passed in this retained source copy. Only package-lock.json differs from the approved 177-file source snapshot; package.json is byte-identical.

This folder is an external review candidate. Nothing here has been transferred, committed or published to NodeVoice. The original checkout and its external HANDOFF proposal remain protected.

Run `python verify-freeze.py` from this folder to check the frozen raw evidence and source bytes. That verifier checks hashes, not fresh runtime behavior. For a new ordinary check in a separate copy of consumer, use Node.js 22.12+ and run `npm ci`, `npm run doctor`, `npm run proof`, and `npm run build`. The manifest preserves the installed seven-package file inventory; node_modules itself is retained locally but excluded from the small evidence manifest.

The existing canary script loads the real installed packages and uses only generated scratch fixtures. Its baseline and proposed output directories are immutable. The before/after verdict records two bounded ID hangs, permitted and forbidden map reads, inherited-key stats, and a sustained 2,050-query cache case. The same observer ran on both cohorts. The cache heap samples are observations, not a performance guarantee.

All three rebuilt browser assets are byte-identical to the earlier independently reviewed UI build. The previous actual 100/100 playback remains historical evidence bound to those exact assets. This slice did not run new browser playback, microphone, shared Convex, provider or durable-room actions. Full visual grades, main-pane/short-height and selected-option limits, audio and deployed behavior remain open.

The fresh full and omit-dev npm audits returned zero findings. That describes the current audit feed and this lockfile, not absence of all vulnerabilities. The changed rows are development tooling; a production-only install is not certified because the documented start command uses devDependency tsx.

Primary source metadata and the original audit findings remain in the operator packet E6j-nodevoice-dependency-recon-01 and the accepted plan. No environment names or values are included in runtime summaries. The copy includes the tracked public .env.example only; no private configuration was copied or read.
