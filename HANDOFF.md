# NodeVoice developer and user handoff

## Current candidate: readable public lobby and crawler documents

A visitor arriving from search now receives the existing room lobby, its styles and canonical metadata in the first HTML response. Previously the public homepage, robots and sitemap all returned the same 862-byte HTML shell; the page was blank without scripts. The candidate prerenders the real lobby through Vite, hydrates the public root, and preserves the existing `/demo` and `?room=...` client routes. Room controls stay disabled until hydration, with a visible explanation when scripts are unavailable. The shared-goal and room-code controls have accessible names, and the lobby has a main landmark.

The public canonical remains `https://nodevoice.vercel.app/`. `robots.txt` and `sitemap.xml` are real files excluded from the app rewrite and served with text/XML content types by the local Node server. The sitemap lists only the public homepage. Crawler exclusions for demo/API/room links are indexing guidance, not authorization or a guarantee that a URL will never be indexed.

Use the keyless first-run commands below. `npm run doctor` passes all three TypeScript projects and 56 citations. `npm run proof` passes 60 tests in 10 files plus the deterministic CLI illustration (nine committed steps with the default target of 12). `npm run build` passes and exits normally. CI now includes the previously omitted Convex typecheck and build. The five new public-page scenarios in [publicPage.test.ts](tests/publicPage.test.ts) exercise actual built responses, crawler routing, 64 repeated/concurrent visits, both transport selections, and child-process build termination.

The retained local browser proof passed 35 scenarios at widths 320, 390, 834, 1280 and 1440: keyboard controls, no scripts, delayed scripts, two real local participants, missing-room refusal, an explicitly injected create failure and recovery, and repeated navigation through the deterministic demo. Each scenario captured DOM, console and pixels; the five lobby Axe scans had zero violations, with manual-review items retained. The missing-room and injected-failure scenarios correctly logged their 404/503 responses. Development rendering separately passed 24 concurrent/repeated responses and normal/no-script browser checks. An actual Convex-selected production build rendered the lobby and exited without any fetch or WebSocket call; this is rendering evidence, not hosted-provider execution.

The security patch changes nine development lock records: the eight coupled Vitest packages to 4.1.11 and baseline-browser-mapping to 2.11.0. Ordinary `npm ci` and a fresh full audit pass with zero findings. All 44 production lock records, 19 installed production packages and package.json are unchanged; rebuilt public HTML/CSS/JS/crawler files are byte-identical to the browser-tested build. The earlier installation and failed resolver/build attempts remain preserved in the operator's evidence packet. These audit results describe the current feed and lock, not complete security.

The renderer imports only the lobby and shared provider composition. Keep the comparison route imports in the browser entry: importing its assistant-ui tracing scheduler in the build renderer created a referenced MessagePort and kept an otherwise successful build running. The scenario suite catches that process-lifetime regression; do not mask it with forced process exit.

This is a branch candidate. Review and canonical publication require exact-commit CI and the public verification steps below. Local screenshots and checks do not certify the production alias. Full visual/responsiveness/interaction/accessibility/performance/usage/alignment grades remain unassigned: narrow profile labels still truncate, and the goal textarea scrolls at the narrowest width. Detailed room/demo panes, physical devices, microphone/audio, real providers and durable hosted restart retain the limits below. Independent review of this slice was unavailable because of the account usage limit; author readback is not independent approval. The older evidence verifiers below describe their historical source bindings and must not be presented as certification of this candidate.

## Previous accepted controls, dependencies and first run

Read this first to try the local comparison or continue the reviewed controls repair. A developer comparing voice-agent designs can run the same count with three private transcripts and with one shared room: the scripted private states remain at 1, while the shared reducer advances to 100. The local demo proves that coordination example; it does not certify a real microphone, external model or durable hosted room.

The reviewed controls repair merged in [PR 6](https://github.com/HomenShum/NodeVoice/pull/6). The seven-row tooling dependency repair then merged in [PR 7](https://github.com/HomenShum/NodeVoice/pull/7) as `41225464a64193c3ceb0a323734be1385724a7ca`; its [main CI](https://github.com/HomenShum/NodeVoice/actions/runs/34000557173) and [conformance](https://github.com/HomenShum/NodeVoice/actions/runs/34000557405) passed. The public project's missing Git connection was restored to this repository, preserving its domain, protection and build settings. The first production deployment of that reviewed main completed on 2026-09-06 at 00:16:57 UTC. At 00:18 UTC, fresh public HTML loaded `index-DhgeGANP.js` with the repaired controls. The [public comparison](https://nodevoice.vercel.app/demo) was observed at 390x844 and 1440x960 with complete control boxes inside the viewport; three phone Tab/Shift+Tab moves reached the numeric fields. This is public controls and delivery evidence, with no new count-100 playback, microphone, provider or durable room run. The existing README's hosted demonstrations and model measurements remain historical. Use [START_HERE](docs/START_HERE.md) for the code walkthrough after this first run.

Use Node.js 22.12+ (or a supported newer LTS), npm, and a fresh checkout with no private `.env.local` or inherited provider/endpoint configuration. The installed Vite 8.1.3 requires `^20.19.0 || >=22.12.0`. No key is needed for this path.

```powershell
npm ci
npm run doctor
npm run proof
npm run build
$env:SOURCE = 'deterministic'
$env:USE_OLLAMA = '0'
npm run start
```

Open `http://localhost:8787/health` first. Both `live.openai` and `live.elevenlabs` must be false for the keyless observation. If either is true, stop this server and use a clean shell/configuration before testing the missing-key path. Do not paste keys into the browser. The current server listens with no explicit host; the reviewed run observed `::` and `0.0.0.0`, so localhost usage is not proof of loopback-only binding. Stop your own server with Ctrl+C when finished.

1. Open `http://localhost:8787/demo` (the bare root is the room lobby). Keep Source at its scripted setting, N 100 and Turns 100. Click **Run the comparison** once. Wait for the visible 100/100, **complete**, and the bottom **Run** button to become available again. The recorded final run took 125.736 s to show 100 and 126.829 s to finish; the proof budget stayed 300 s. These are observations on one machine, not a latency guarantee or audio-quality certification.
2. On a phone-sized viewport, each complete control now wraps into ordinary rows. Use Tab and Shift+Tab through the controls, then set each numeric field to 99 and use its native up spinner or ArrowUp to return to 100. The selection labels can still shorten with ellipses.
3. Separately open `/` and create a Count 10 room. While it is waiting for the second participant, toggle **Invite** and **State** with pointer or keyboard. Invite's QR visibility still depends on waiting for another participant; this proof does not promise the same QR toggle after all seats fill.
4. In a second local browser context, join the displayed code. Enter `Actually, count from 5 to 10`. The deterministic correction retargets the task, then automatic resume visibly stops on the missing-key error. One explicit **Start** produces one additional error with no completed turn. This is expected failure disclosure, not successful provider execution. Navigating back to `/` restores the lobby while the local server remains alive; durable recovery across restart was not tested.

`npm run doctor` passed all three TypeScript projects and 56 citations. `npm run proof` passed 55 tests in 9 files plus the CLI smoke. The CLI's actual defaults are target 12 / nine turns; that smoke is separate from the real browser 100 proof. Normal build passed. Merged-controls CI uses Node 20 and passed its server/client checks, 56 citations and 55 tests; it omits the Convex typecheck, build and full browser matrix. Its CLI smoke uses target 10 but commits nine steps with next=10. The dependency candidate separately passed normal install, all three doctor typechecks, 55 tests, its target-12/nine-turn CLI illustration and build in the retained external proof. Those results remain source-bound and independently reviewed. Dependency main CI subsequently passed the same 55-test/nine-file server/client scope and 56 citations; its install reported zero findings. That CI still omits Convex types, build and the native browser matrix. Hosted controls observations are separate from the local count-100 proof.

The [scoped evidence packet](promotion/evidence/controls-20260905/README.md) contains the current 337-check / 94-capture raw report, selected actual pixels, structured state, ordinary logs, source hashes, and the retained failed scroll approaches. Run its standard-library verifier with `python promotion/evidence/controls-20260905/verify.py`. Optional `--source-root .` checks the historical 177-file UI binding, including the old lock, and therefore correctly fails after this dependency update; the packet-only command still passes. Use `python promotion/evidence/dependencies-20260905/verify.py --source-root .` for the effective 177-file dependency source binding. It does not require Git or rerun the browser. Exact old browser replay remains operator-local; [the existing count script](scripts/prove-count-to-100.mjs) has a narrower oracle and writes a fixed historical output directory, so it is not a substitute for the retained 337-check proof.

Limits remain open: detailed comparison/room panes and short-height reading, selected-option ellipsis, full visual/accessibility grades, physical devices, native zoom (the evidence doubles computed font sizes), microphone and audible speech quality, real provider/agent execution, hosted Convex and durable restart. The historical UI packet retains its original three high dependency entries. The [dependency supplement](promotion/evidence/dependencies-20260905/README.md) records the accepted updates, fresh full and omit-dev audits with zero findings, actual before/after failure canaries and the independent 60-check judgment. Its exact rebuilt HTML/CSS/JS match the earlier local 100 proof; no new playback was performed. Audit zero describes the current feed and lock, not complete security or a production-only install: the start command still uses devDependency tsx. The seven dependency rows change no application code, package.json ranges, backend policy, state handlers or publication configuration.


## Public delivery and handoff

The existing Vercel project is **nodevoice**, connected to **HomenShum/NodeVoice**, with production branch **main** and canonical domain **nodevoice.vercel.app**. Keep root `.`, Vite, `npm run build`, output `dist`, the existing `npm install` setting and Node 24.x. The separate protected local-collab-mvp project is not evidence that the canonical public alias updated.

1. Submit a normal reviewed PR and inspect its actual CI results. Preserve explicit skips and the limits above.
2. Merge normally. The restored Git integration is configured to deploy production pushes to main; verify that an exact-commit deployment was actually created and reached Ready.
3. Check that the nodevoice project assigns the canonical domain to that deployment. Fetch the public HTML and its actual entry module, and find a concrete token from the changed surface. HTTP 200 alone can be a stale bootstrap or login page.
4. Open the affected public view in a browser. Capture DOM, console and settled pixels at each required viewport before resizing again. The retained first desktop capture had old phone paint despite new DOM geometry; only its settled replacement was accepted. The observed model listing was deterministic/hosted/production, not proof of provider execution.
5. Record the source commit, actual checks, deployment identity, observed user behavior and remaining limits in the handoff. Keep the previous verified deployment available for ordinary rollback if the real public behavior fails.

The connection repair used the existing signed Git settings picker without expanding repository permissions or downloading environment values. Do not use automatic CLI linking as an environment-copy shortcut: the observed CLI 50.4.11 `link --yes` also pulls development values. Keep credentials in their existing server-side environment; this handoff does not authorize moving the NodeBench Convex key. The public verification retained six extension-origin console warnings and no application-origin warning/error entries. Complete visual/accessibility grading, selected-label ellipsis and the other limits above remain open.
